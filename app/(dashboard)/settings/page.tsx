import type { FC } from 'react';

import { DashboardSection } from '@/src/components/layout/dashboard';

const SettingsPage: FC = () => {
  return (
    <main>
      <DashboardSection>Settings</DashboardSection>
    </main>
  );
};

export default SettingsPage;
